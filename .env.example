# Optional overrides for docker-compose.yml and anvil/docker-compose.yml
# Copy to .env to customize locally:
#   cp .env.example .env

POSTGRES_USER=indexer
POSTGRES_PASSWORD=indexer_password
POSTGRES_DB=indexer

# Default fork URL (you can override if needed)
# Public alternative: https://eth.llamarpc.com
ANVIL_FORK_URL=https://reth-ethereum.ithaca.xyz/rpc

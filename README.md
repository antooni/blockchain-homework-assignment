# How to run

1. Start anvil
```sh
cd anvil
docker compose up -d
```

2. Start docker with infra
```sh
docker compose up -d
```

3. .env file includes everything that is needed

4. Build & run
```sh
npm install
npm run build
npm run start
```
